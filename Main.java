import java.io.File;
import java.nio.file.Paths;

/**
 * By running this main you will see an image and its effects
 * Uncomment the code once you have implemented your project
 *
 * If the relative paths do not work, change the content of
 * "pathToMyScriptsDir" and "pathToMyImage" to absolute paths
 * in your working environment
 *
 * @author Joseph Lewis joseph@josephlewis.net
 * @version 2/25/16
 */
public class Main {

    public static void main(String[] args) {
        /*
        EffectsList effects = new EffectsList();


        String pathToMyScriptsDir = "./scripts";
        for(File f : Paths.get(pathToMyScriptsDir).toFile().listFiles()) {
            if(! f.isFile() || !f.getName().endsWith(".script"))
                continue;
            AdjustColor effect = ScriptProcessor.processScript(f);
            if(effect != null)
                effects.add(effect);
        }


        // Create the viewer that holds the effects we need
        String pathToMyImage = "./image.jpg";
        new ImageViewer(effects, pathToMyImage);

        */
    }
}
